Usa el comando `git init` para crear un nuevo repositorio en el directorio actual.

```
git init
```

Correr este comando crea el directorio `.git`. Este directorio es el centro del repositorio, se almacenan las configuraciones y el registro de los commits almacenados.